name: CI
on: 
 schedule:
    - cron: "0 */2 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: CI
        run: |
         curl --request POST \
          --url 'https://api.netlify.com/build_hooks/6499a8547aeaae00b5315025' \
